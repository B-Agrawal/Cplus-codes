#include<iostream.h>
#include<conio.h>
#include<ctype.h>
#include<string.h>
void newch(char m[],int &loc)
{ char *p=m;

  for(;loc<strlen(m)-2;loc+=2,p++)
   {
     *(p+loc)=toupper(*(p+loc));
     }
     }
void main()
{
 clrscr();
 char x[]="Dynamic Act";
int loc=0;
 newch(x,loc);

  cout<<x<<'#'<<loc;
  getch();

}
