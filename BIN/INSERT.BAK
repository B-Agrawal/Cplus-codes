#include<stdio.h>
#include<conio.h>
void main()
{
 int a[10],num,pos,i,n;
 clrscr();
 printf("\nenter array");
 scanf("%d",&n);
 for(i=0;i<n;i++)
   scanf("%d",a[i]);


 printf("\nentr num to insert :");
 scanf("%d",&num);
 printf("\nntr position :" );
     scanf("%d",&pos);
     n++;
  for(i=n;i>pos;i--)
     {
      a[i]=a[i-1];
      }
   a[pos]=num;
   printf("\n");

   for(i=0;i<n;i++)
   printf(" %d",a[i]);
   getch();
   }

